#include <stdio.h>
#include <stdlib.h>

typedef struct element()
{
  int number;
  struct element* next;
} Element;

Element* addElement(Element* list, int value){
  if (Element->next==NULL){
	list=malloc(sizeof(Element))
	list->number=value;
  }
}


int main()
{
  int i=-1;
  while(i!=0){
	scanf("%d", &i);
	if (Element->next==NULL){
	   
	}
  }
  return 0;
}

